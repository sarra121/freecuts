/**
 * Cross-feature import seam (contract) for preview -> shapes-konva.
 *
 * Per `check:deps-contracts`, the raw `@/features/shapes-konva` import lives
 * here; the sibling `shapes-konva.ts` adapter re-exports from this file.
 */
export { ShapesStage } from '@/features/shapes-konva'
